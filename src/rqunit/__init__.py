"""RU framework spec tooling.

The normative artifacts (framework spec, formats reference, JSON Schemas)
live in ``spec/framework/`` — this package loads them from there and never
carries copies (P8: one fact, one place).
"""
