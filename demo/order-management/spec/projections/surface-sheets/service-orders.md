# Surface sheet — service-orders
<!-- GENERATED by spec-generate — DO NOT EDIT (regenerate and commit; hand-edits fail `spec-generate check`) -->

### service-orders (manifest hash sha256:7d8da7682078…)

| method | path | access | planned | ru |
|---|---|---|---|---|
| POST | /api/v1/orders | protected |  | FEAT-order-placement |
| DELETE | /api/v1/orders/{order_id} | protected |  | FEAT-order-cancellation |
| POST | /api/v1/orders/{order_id}/delivery | carrier |  | FEAT-order-delivery |
| GET | /api/v1/orders | protected | yes | FEAT-order-placement |
| GET | /api/v1/orders/{order_id} | protected |  | FEAT-order-placement |
| POST | /api/v1/sessions | public |  | FEAT-order-placement |
| POST | /api/v1/sessions/refresh | refresh |  | FEAT-order-placement |

**place_order · inbound** — undeclared fields: reject

- `sku` (required, in body, string)
- `quantity` (required, in body, integer, min 1)
- `idempotency_key` (required, in body, string)
- `total_cents` (forbidden) — the price is ours to compute, the same way the refund is

**place_order · outbound** — status 201

- `order_id` (always, string)
- `state` (always, string)
- `total_cents` (always, integer)

**cancel_order · inbound** — undeclared fields: reject

- `order_id` (required, in path, string)
- `reason` (required, in body, string, values ∈ cancellation_reasons)
- `refund_cents` (forbidden) — the client sent this once; we work it out

**cancel_order · outbound** — status 204 — declared empty: carries no body.

**record_delivery · inbound** — undeclared fields: reject

- `order_id` (required, in path, string)
- `outcome` (required, in body, string, values ∈ delivery_outcomes)
- `signed_at` (required, in body, string)

**record_delivery · outbound** — status 204 — declared empty: carries no body.

**list_orders · inbound** — undeclared fields: reject

- `page_size` (optional, in query, integer, max {value:paging.max_page_size})

**list_orders · outbound** — status 200

- `items` (always, array, of object)
- `items.order_id` (always, string)
- `items.state` (always, string)
- `next_page` (always, string, nullable)

**get_order · inbound** — undeclared fields: reject

- `order_id` (required, in path, string)

**get_order · outbound** — status 200

- `order_id` (always, string)
- `state` (always, string)
- `cancellation` (always, object, nullable)
- `cancellation.at` (always, string)
- `delivery` (always, object, nullable)
- `delivery.outcome` (always, string, values ∈ delivery_outcomes)
- `delivery.at` (always, string)
- `cost_basis` (never) — internal pricing never leaves

**issue_session · inbound** — undeclared fields: reject

- `email` (required, string, max_chars 254)

**issue_session · outbound** — status 201

- `access_token` (always, string)
- `refresh_token` (always, string)

**refresh_session · inbound** — declared empty: carries no body.

**refresh_session · outbound** — status 201

- `access_token` (always, string)

| direction | subject | payload | ru |
|---|---|---|---|
| outbound | orders.placed | wire::OrderPlaced | FEAT-order-placement |
| outbound | orders.cancelled | wire::OrderCancelled | FEAT-order-cancellation |
| outbound | orders.delivered | wire::OrderDelivered | FEAT-order-delivery |
| inbound | billing.refund_settled | wire::RefundSettled | FEAT-order-cancellation |

