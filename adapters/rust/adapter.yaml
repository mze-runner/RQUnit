# Adapter self-declaration (contract: rqunit interfaces/adapter-manifest.schema.json).
# Read by core, never by this adapter.
contract_version: 1
stack: rust
roles: [extractor, scanner, emitter]
config_keys:
  # Read by extract.rs.
  - service
  - routers
  - messages
  - audit
  # Read by scan.rs.
  - trace_scan
  # Read by emit.rs (arrives as request data, not from disk).
  - conformance_crate
