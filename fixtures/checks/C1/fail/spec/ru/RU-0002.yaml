# Bound conflict: one obligation, two numbers. The trigger phrasing is
# reordered, which C1 collapses — the disagreement is real, not wording.
id: RU-0002
statement: >
  When {endpoint:cancel_order} is called by a user, the system shall halt fulfilment for the order within 30 seconds.
syntax: ears
status: active
source_ref: INT-0001#L1-2
verification:
  - { type: test, ref: TODO(not yet proved) }
scope:
  owns: [service-orders/fulfilment]
tags: [orders]
