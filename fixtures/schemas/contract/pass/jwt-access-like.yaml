id: CT-jwt-access-token
kind: claim-set
description: Full access token claim set, header placement, and absences.
access_tier: protected
fields:
  - { name: sub, where: claims, presence: always, type: string, note: user id }
  - { name: jti, where: claims, presence: always }
  - { name: kid, where: header, presence: always }
  - { name: iss, presence: never }
  - { name: scope, presence: never }
